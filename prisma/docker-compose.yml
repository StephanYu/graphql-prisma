version: '3'
services:
  prisma:
    image: prismagraphql/prisma:1.34
    restart: always
    ports:
    - "4466:4466"
    environment:
      PRISMA_CONFIG: |
        port: 4466
        # uncomment the next line and provide the env var PRISMA_MANAGEMENT_API_SECRET=my-secret to activate cluster security
        # managementApiSecret: my-secret
        databases:
          default:
            connector: postgres
            host: ec2-34-248-165-3.eu-west-1.compute.amazonaws.com
            database: d5j2vc2fcs1dod
            user: dmruradyrjulrm
            password: 7fe9a891a3fce318957c64644691f8cb829b62cd295cc355886c964ac1a98314
            ssl: true
            rawAccess: true
            port: '5432'
            migrations: true